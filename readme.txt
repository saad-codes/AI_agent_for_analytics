First install libraries with 
pip install -r .\req.txt  
Now if you want to test or see example implemetation you can use example_implementation.ipynb
and in main_file we call agent so open that and run it will start the agent and present the results 
